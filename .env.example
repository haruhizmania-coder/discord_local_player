# Discord Developer Portal で発行した Bot トークン(必須)
DISCORD_TOKEN=

# スラッシュコマンドを即時反映したいサーバーの ID(任意。未設定だとグローバル同期で反映に最大1時間かかる)
GUILD_ID=

# 音源フォルダ(任意。デフォルトは ./sounds)
# SOUNDS_DIR=sounds

# PATH に ffmpeg が通っていない場合のフルパス(任意)
# FFMPEG_PATH=C:\ffmpeg\bin\ffmpeg.exe
